import styled from 'styled-components'
import { Table } from '../ActivitiesTable/style'

export const Category = styled.p`
  font-weight: bold;
  text-transform: capitalize;
`

export const TableHours = styled(Table)`
  max-width: 70rem;
  align-self: center;
  margin: 0 auto;
`
